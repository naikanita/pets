<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Pets" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Pets" parent="Base.Theme.Pets" />

    <style name="Theme.Pets.Fullscreen" parent="Theme.Pets">
        <item name="android:actionBarStyle">@style/Widget.Theme.Pets.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.Pets.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/black_overlay</item>
        <item name="fullscreenTextColor">@color/white</item>
    </style>

    <style name="ToolbarTheme" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:fontFamily">casual</item>
        <item name="android:textColor">@color/white</item>
        <item name="background">@color/black</item>
    </style>
</resources>